# da-patcher
Preloader and DA patching crate to disable various security features.

## GUI
Run debugging GUI with `cargo r --release --features gui --bin da-patcher-gui`

Hotkeys:
  - *P* - patches window (only Thumb2 patches are properly supported)
  - *G* - goto address (without 0x)
